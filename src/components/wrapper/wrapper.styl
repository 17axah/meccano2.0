.wrapper
  max-width 320px
  margin 0 auto
  padding 0 6px

  +above($breakpoints.sm)
    max-width 750px
    margin 0 auto
    padding 0 12px

  +above($breakpoints.md)
    max-width 996px
    padding 0 15px

  +above($breakpoints.xxl)
    padding 0 20px
    max-width 1220px
